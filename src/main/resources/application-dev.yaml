spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:conflicts;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  sql:
    init:
      mode: always
      encoding: UTF-8
